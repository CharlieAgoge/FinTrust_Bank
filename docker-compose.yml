services:
  fintrust-web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - CONTAINER=1
    # Optional: persist SQLite for dev (uncomment to keep data across restarts)
    # volumes:
    #   - fintrust-data:/app/instance
#
# volumes:
#   fintrust-data:
